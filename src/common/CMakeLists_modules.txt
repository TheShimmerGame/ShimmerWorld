
set(TARGET_NAME common_shim)

add_library(${TARGET_NAME} STATIC)
add_library(shimmer::common ALIAS ${TARGET_NAME})

shimmer_glob_module_sources(MODULE_SOURCE_FILES)

target_sources(${TARGET_NAME}
    PUBLIC
        FILE_SET CXX_MODULES
        TYPE CXX_MODULES
        BASE_DIRS ${CMAKE_CURRENT_SOURCE_DIR}
        FILES ${MODULE_SOURCE_FILES}
)

#source_group(TREE "${CMAKE_CURRENT_SOURCE_DIR}" FILES ${MODULE_SOURCE_FILES})

set_target_properties(${TARGET_NAME} PROPERTIES
    CXX_SCAN_FOR_MODULES ON
)
